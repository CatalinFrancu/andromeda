# andromeda
An engine for the board game Ataxx.

## Installation

```bash
git clone https://github.com/CatalinFrancu/andromeda
```

Copy `include/Config.h.sample` to `include/Config.h` and edit if needed.

```bash
cd andromeda/build
cmake ..
make
```
